apiVersion: v2
name: parches
description: PATRICIA Parches MS (8083, a.k.a. parche-core in infra naming) — deployed by the CD job in .github/workflows/CI.yml on push to main
type: application
version: 0.1.0
appVersion: "0.0.1"
